name: fl_umeng
description: umeng plugin for flutter, only Android and IOS are supported
version: 0.0.1
repository: https://github.com/Wayaer/fl_umeng

environment:
  sdk: '>=2.18.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
