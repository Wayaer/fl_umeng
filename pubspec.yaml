name: fl_umeng
description: umeng plugin.
version: 0.0.1
homepage: https://github.com/Wayaer/flutter_umeng.git

environment:
  sdk: ">=2.1.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: flutter.umeng
        pluginClass: UmengPlugin
      ios:
        pluginClass: UmengPlugin
